{
  "name" : "builtin:dungeon_grassland_no_loot",
  // does not spawn
  "exclusive": false,
  "inherit" : ["builtin:grassland"],
  // remove the loot, so that extending dungeons (dungeons that inherit this setting) can supply their own loot
  "overrides": ["LOOTRULES"]
}
