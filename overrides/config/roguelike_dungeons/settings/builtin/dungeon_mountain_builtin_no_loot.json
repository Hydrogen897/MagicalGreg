{
  "name": "builtin:dungeon_mountain_no_loot",
  // does not spawn
  "exclusive": false,
  "inherit": ["builtin:mountain"],
  // remove the loot, so that extending dungeons (dungeons that inherit this setting) can supply their own loot
  "overrides": ["LOOTRULES"]
}
